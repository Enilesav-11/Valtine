
  # Valentine's Invitation with Response

  This is a code bundle for Valentine's Invitation with Response. The original project is available at https://www.figma.com/design/zGgeINVbB7sEEyM2DIRaZG/Valentine-s-Invitation-with-Response.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  